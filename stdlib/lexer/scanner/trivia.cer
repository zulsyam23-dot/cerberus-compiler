program lexer_scanner_trivia_mod;
uses lexer_core;
function lex_sc_is_space(ch: string): boolean;
begin
  return lex_is_space(ch);
end;
function lex_sc_is_comment_start(ch: string): boolean;
begin
  return ch = '#';
end;
begin
end.
